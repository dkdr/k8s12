kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: api-allow
spec:
  podSelector:
    matchLabels:
      app: mysql
      role: database
  ingress:
  - from:
      - podSelector:
          matchLabels:
            role: backend